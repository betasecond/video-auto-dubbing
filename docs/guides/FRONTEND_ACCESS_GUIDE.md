# 🌐 前端设置界面使用指南

## ✅ 服务状态确认

### 当前运行的服务
- **前端界面**: ✅ http://localhost:3000/
- **后端 API**: ✅ http://localhost:8080/api/v1/

### 服务检查
```bash
# 检查前端服务
curl -s http://localhost:3000/ | head -n 3

# 检查 API 服务
curl -s http://localhost:8080/api/v1/settings | head -n 3
```

## 🎯 设置界面访问步骤

### 1. 打开前端界面
```
在浏览器中访问: http://localhost:3000/
```

### 2. 进入设置页面
```
1. 页面加载后，点击页面右上角的 "设置" 按钮
2. 设置面板会以弹窗形式打开
```

### 3. 配置 ASR 服务
```
1. 点击 "ASR 服务" 标签页
2. 查看已配置的火山引擎设置：
   - App Key: 608***513 (已脱敏显示)
   - Access Key: LW8***oGu (已脱敏显示)
   - 资源 ID: volc.bigasr.auc
   - 识别选项: 全部启用
3. 点击 "测试连接" 验证 ASR 服务
```

### 4. 配置 TTS 服务
```
1. 点击 "TTS 服务" 标签页
2. 填写服务地址: https://u861448-ej47-562de107.bjb2.seetacloud.com:8443
3. 后端类型: VLLM 后端 (推荐)
4. 点击 "测试连接" 验证 TTS 服务
```

### 5. 保存设置
```
1. 配置完成后点击 "保存设置" 按钮
2. 系统将显示保存成功提示
3. 设置会自动保存到数据库
```

## 🧪 功能测试结果

### ASR 服务 (火山引擎)
- ✅ **连接状态**: 已连接
- ✅ **认证测试**: 通过
- ✅ **功能**: 说话人分离、情绪检测、性别检测等

### TTS 服务 (IndexTTS)
- ✅ **连接状态**: 已连接
- ✅ **服务类型**: Gradio IndexTTS
- ✅ **API 端点**: 5 个可用 (包括 /gen_single)
- ✅ **功能**: 多情感控制、音色克隆等

## 🎨 界面功能特色

### 设置界面特性
- **🎨 现代化设计**: 响应式布局，支持各种设备
- **⚡ 实时测试**: 配置后立即验证连接状态
- **🔒 安全显示**: 敏感信息自动脱敏
- **✨ 状态指示**: 连接状态实时显示 (绿点/红点)
- **📋 分类管理**: ASR、TTS、翻译服务分别配置

### 用户体验
- **简单易用**: 图形化配置，无需技术背景
- **即时反馈**: 保存、测试结果立即显示
- **错误提示**: 详细的错误信息和解决建议
- **快速设置**: 预设默认值，减少配置工作量

## 🚨 注意事项

### 如果无法访问前端
```bash
# 检查服务是否运行
ps aux | grep "python3 -m http.server"

# 重新启动前端服务
cd /Users/micago/Desktop/index/video-auto-dubbing/web
python3 -m http.server 3000 &
```

### 如果 API 无响应
```bash
# 检查 API 服务
ps aux | grep "test_api_fixed.py"

# 重新启动 API 服务
cd /Users/micago/Desktop/index/video-auto-dubbing
python3 test_api_fixed.py &
```

### 端口冲突处理
- **前端端口**: 3000 (可改为 3001, 3002 等)
- **API 端口**: 8080 (可改为 8081, 8082 等)

## 📱 移动设备访问

如果需要在其他设备访问：
```bash
# 获取本机 IP 地址
ifconfig | grep "inet " | grep -v 127.0.0.1

# 使用 IP 地址访问 (示例)
# http://192.168.1.100:3000/
```

---

## ✅ 快速开始

1. **直接访问**: http://localhost:3000/
2. **点击设置**: 页面右上角 "设置" 按钮
3. **测试服务**: 分别测试 ASR 和 TTS 连接
4. **保存配置**: 确认无误后保存设置

现在你就可以完全通过 Web 界面管理你的视频配音系统设置了！🎉